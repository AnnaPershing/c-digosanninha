#include <stdio.h>

int main() {
    
 int i , n, aux=0; // variaveis
    for(i = 1; i <= 5 ; i++){ // leitura dos 5 numeros
      scanf("%d", &n);
      if (n % 2 == 0) // se for par, contar
        aux++;
    }
    printf("%d valores pares\n",aux); // quantidade de pares
    
    return 0;
}
