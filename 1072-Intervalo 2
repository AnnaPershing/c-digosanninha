#include<stdio.h>

int main(){
  
    int n, x, i;
    int in=0, out=0; // variaveis
    scanf("%d", &n); // numero de casos de testes
    for (i = 0; i < n ; i++){
      scanf("%d", &x); 
      if((x >= 10) && (x <= 20)){
        in++; // soma dos que estao dentro
      }
      else 
       out++; // soma dos que estao fora
    }
    printf("%d in\n", in); // dentro
    printf("%d out\n",out); // fora
    return 0;
}
